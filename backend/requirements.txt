# Versions pinned to a set verified on Python 3.13 (older pins lack 3.13 wheels)

# Web framework
fastapi==0.141.1
uvicorn[standard]==0.53.0   # [standard] pulls in websockets — required for /ws

# Database
sqlalchemy==2.0.54
asyncpg==0.31.0
greenlet==3.5.6
alembic==1.20.0

# Auth
python-jose[cryptography]==3.5.0
bcrypt==4.2.0
python-multipart==0.0.32

# HTTP client (Judge0 proxy)
httpx==0.28.1

# WebSockets
websockets==17.1

# Validation
pydantic==2.13.5
pydantic-settings==2.15.0
email-validator==2.3.0

# Cheating detection / NLP
numpy==2.5.3
scikit-learn==1.9.1

# Env
python-dotenv==1.2.3

# Testing
pytest==9.1.1
